---
'@gpuiv/vue': minor
---

Add a `Spinner` loading primitive to `@gpuiv/vue`: three pulsing dots by default, or an indeterminate sliding bar with `variant="pulse"`. Size, color, track width, and the accessibility label are props, and `phase` pins the animation for deterministic screenshots and tests.
