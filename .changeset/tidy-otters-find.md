---
'@gpuiv/vue': minor
---

Add `TestRenderer.findByTestId()` and expose `testId` on `TestElement`, so tests can locate marked elements and click their painted bounds instead of fixed window coordinates.
