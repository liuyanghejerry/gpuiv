---
'@gpuiv/native': patch
'@gpuiv/vue': patch
---

Improve `<markdown-editor>` editing consistency: support drag selection across blocks, use a natural full-line caret, let source documents fill and scroll the editor, show a centered checkmark in selected task items, preserve Markdown during copy and cut, switch list types without nesting, and include table cells in search and the shared editing pipeline.
