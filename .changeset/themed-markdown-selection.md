---
'@gpuiv/native': patch
'@gpuiv/vue': patch
---

Use the Markdown theme's selectionColor for local, cross-block, table, and source selections. Paint selections above inline code and highlight backgrounds so selected text remains visibly selected. Match the Elegant example's selection to its warm brick-red palette.
