import type { PackageConfig } from "@gpuiv/packager"

export default {
  entry: "./single-instance.tsx",
  productName: "GPUIV Single Instance",
  bundleId: "dev.gpuiv.single-instance-example",
  version: "0.1.0",
} satisfies PackageConfig
